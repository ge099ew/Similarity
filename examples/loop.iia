# Loop の例（カウントダウン）

Function_public[main{
  Variable[let{int(i:10)}],
  Variable[let{int(sum:0)}],
  Loop[
    check{greater(i:0)},
    for{
      Mutation[variable{int(sum:+{int(sum,i)})}],
      --{i}
    }
  ],
  return(sum)
}]
