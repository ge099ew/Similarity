// Similarity サンプルコード
// このファイルで動作確認をする

Explanation[Application{Game(type:RPG, name:TestGame)}]

Import[mylib{}]

Variable[let{int(hp:100)}]
Variable[unchanging_let{int(MAX_HP:100)}]
  
Function[heal{
  receive{int(amount)},
  If[
    check{le(hp, 0)},
    True[Fatal[type{GameOver}, msg{"HPが0以下"}]],
    False[Variable[let{int(hp:+{int(hp, amount)})}]]
  ],
  return{}
}]

Function_public[main{
  receive{},
  call{heal(10)},
  return{}
}]
