Explanation[Application{Benchmark(type:sumloop)}]

Function_public[main{
  receive{},
  Variable[let{int(sum:0)}],
  Loop[
    check{lt(i,100000000)},
    for{
      Variable[let{int(i:0)}],
Variable[let{int(sum:+{int(sum,i)})}],
      ++{i}
    }
  ],
  return{sum}
}]
